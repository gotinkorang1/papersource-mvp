import { NextResponse } from "next/server";
import { recordAdminAudit } from "@/features/admin/audit";
import { savePage } from "@/features/content/admin";
import { readStaffActor } from "@/lib/staff/require";
export async function POST(request: Request) { const origin = new URL(request.url).origin; const next = new URL("/admin/pages", origin); const actor = await readStaffActor(); if (!actor) return NextResponse.redirect(new URL("/admin/login", origin), 303); const form = await request.formData(); const id = String(form.get("id") || "") || undefined; try { await savePage(actor.role, id, { slug: String(form.get("slug") || ""), title: String(form.get("title") || ""), description: String(form.get("description") || ""), body: String(form.get("body") || ""), status: String(form.get("status") || "draft") }); await recordAdminAudit({ actorProfileId: actor.profileId, action: id ? "content_page_updated" : "content_page_created", resourceType: "content_page", resourceId: id }); } catch { next.searchParams.set("error", "Could not save page. Please check the fields and try again."); } return NextResponse.redirect(next, 303); }
