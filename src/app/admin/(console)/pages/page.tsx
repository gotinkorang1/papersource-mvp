import type { Metadata } from "next";
import { AdminError, AdminField, adminAreaClass, adminFieldClass } from "@/components/admin/field";
import { listAdminPages } from "@/features/content/admin";
import { canAccessAdmin } from "@/lib/staff/rbac";
import { requireStaffArea } from "@/lib/staff/require";
export const metadata: Metadata = { title: "Pages" };
export default async function AdminPagesPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const actor = await requireStaffArea("pages", "read"); const rows = await listAdminPages(actor.role); const canWrite = canAccessAdmin(actor.role, "pages", "write"); const { error } = await searchParams; const form = (row?: typeof rows[number]) => <form action="/admin/pages/mutate" method="post" className="grid gap-3 rounded-xl border border-border bg-card p-5 sm:grid-cols-2"><input type="hidden" name="id" value={row?.id ?? ""} /><AdminField label="Slug"><input name="slug" required defaultValue={row?.slug} placeholder="our-story" className={adminFieldClass} /></AdminField><AdminField label="Title"><input name="title" required defaultValue={row?.title} className={adminFieldClass} /></AdminField><AdminField label="Description"><input name="description" required defaultValue={row?.description} className={adminFieldClass} /></AdminField><AdminField label="Status"><select name="status" defaultValue={row?.status ?? "draft"} className={adminFieldClass}><option value="draft">Draft</option><option value="published">Published</option></select></AdminField><AdminField label="Body"><textarea name="body" required defaultValue={row?.body} rows={8} className={`${adminAreaClass} sm:col-span-2`} /></AdminField>{canWrite ? <button className="sm:col-span-2 sm:justify-self-start ${paperButton()}" type="submit">{row ? "Save page" : "Create page"}</button> : null}</form>; return <main><p className="text-sm uppercase tracking-[0.16em] text-slate">Website</p><h1 className="mt-2 font-heading text-3xl text-ink">Pages</h1><p className="mt-3 text-slate">Manage published information pages and keep drafts private.</p><AdminError error={error} /><div className="mt-8 space-y-5">{rows.map((row) => <section key={row.id}><div className="mb-2 flex items-center justify-between text-sm"><span className="font-medium text-ink">/{row.slug}</span><span className="text-slate">{row.status}</span></div>{form(row)}</section>)}{canWrite ? <section><h2 className="mb-2 font-heading text-xl text-ink">New page</h2>{form()}</section> : null}</div></main>; }
