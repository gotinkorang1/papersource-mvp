import { PlaceholderPage } from "@/components/marketing/placeholder-page";

export default function AccountPage() {
  return (
    <PlaceholderPage
      title="Account"
      body="Orders, quotes, Ghana addresses, and organisation membership. Auth is wired in a later phase."
      href="/"
      cta="Back to home"
    />
  );
}
