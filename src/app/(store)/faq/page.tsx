import type { Metadata } from "next";
import Link from "next/link";
import { FALLBACK_FAQS, faqJsonLd, listPublishedFaqs } from "@/features/content";

export const metadata: Metadata = { title: "Frequently asked questions", description: "Answers about PaperSource products, quotations, delivery and payment." };
export default async function FaqPage() { const managed = await listPublishedFaqs(); const items = managed.length ? managed.map(({ question, answer }) => ({ question, answer })) : FALLBACK_FAQS.map(([question, answer]) => ({ question, answer })); return <main className="mx-auto max-w-3xl px-4 py-14 sm:px-6 md:py-20 lg:px-8"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqJsonLd(items)) }} /><p className="text-sm tracking-[0.16em] text-slate uppercase">FAQ</p><h1 className="mt-4 text-4xl text-ink md:text-5xl">Answers before you order.</h1><div className="mt-10 divide-y divide-border rounded-lg border border-border bg-card px-6">{items.map(({ question, answer }) => <section key={question} className="py-6"><h2 className="text-lg font-medium text-ink">{question}</h2><p className="mt-2 text-sm leading-relaxed text-slate">{answer}</p></section>)}</div><p className="mt-8 text-slate">Still have a question? <Link href="/contact" className="text-ink underline">Contact us</Link>.</p></main>; }
